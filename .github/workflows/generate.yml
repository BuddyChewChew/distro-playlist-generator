name: Update DistroTV Playlist

on:
  schedule:
    - cron: '0 */6 * * *' # Runs every 6 hours
  workflow_dispatch:      # Allows manual run
  push:
    branches:
      - main
    paths:
      - 'distro.py'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run Scraper
        run: python distro.py

      - name: Commit and Push Changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          # Since the script now creates a 'playlists' folder, 
          # we add the entire folder instead of individual files.
          git add playlists/
          
          # Only commit if there are changes in the playlists directory
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "🔄 Auto-update DistroTV playlists & EPG [$(date -u +'%Y-%m-%d %H:%M:%S') UTC]"
            git push
          fi
